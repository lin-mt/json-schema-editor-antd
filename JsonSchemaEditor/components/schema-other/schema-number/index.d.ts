import { ReactElement } from 'react';
import Schema from '../../../types/Schema';
interface SchemaNumberProp {
    data: Schema;
}
declare const SchemaNumber: (props: SchemaNumberProp) => ReactElement;
export default SchemaNumber;
