import { ReactElement } from 'react';
import Schema from '../../../types/Schema';
interface SchemaStringProp {
    data: Schema;
}
declare const SchemaString: (props: SchemaStringProp) => ReactElement;
export default SchemaString;
